# Reset CrossOver Trial — How to Use

## First-time setup

1. Open **Terminal** (Applications → Utilities → Terminal).
2. Type `chmod +x ` (with a trailing space — don't hit enter yet).
3. Drag `Reset_RunManager.command` from Finder into the Terminal window.
4. Hit **Enter**.

Done. Now just double-click `Reset_RunManager.command` any time you want to reset the CrossOver trial.
